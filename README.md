# kajit-zed

Zed language support for Kajit.

This minimal extension wires `.vixen-hir` files to `kajit lsp`. It does not
ship a grammar yet; highlighting comes from the language server's semantic
tokens.
